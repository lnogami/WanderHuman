import 'package:flutter/material.dart';

/// A utility class where I define my color palette.
/// This class provides a centralized place to manage colors used in the app.
class MyColorPalette {
  static const Color fontColorB = Color.fromARGB(255, 30, 30, 30);
  // static const Color darkBlue = Color.fromARGB(255, 81, 110, 184); // temporary

  // static const Color titleTextColor = Color.fromARGB(255, 5, 97, 172);
  // static Color subtitleTextColor = Colors.blue;
  static const Color titleTextColor = Color.fromARGB(255, 38, 151, 243);
  static const Color subtitleTextColor = Colors.blue;

  static const Color borderColor = Color.fromARGB(255, 144, 202, 249);

  /// Background color for Forms
  static const Color lightBlue = Color.fromARGB(211, 186, 220, 247);

  /// InkWell's splash color
  static const Color splashColor = Color.fromARGB(180, 55, 164, 253);

  /// Forms's Background Color
  static const Color formColor = Color.fromARGB(255, 240, 246, 255);
}
